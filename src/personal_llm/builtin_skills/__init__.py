"""Built-in skills that ship with the package.

Phase 0: empty. Phase 1 adds web_search (via MCP), read_file, summarize, etc.
The skill registry (L5) merges these with the user's vault-local skills.

All built-in skills follow the Anthropic Agent Skills SKILL.md open standard
(YAML frontmatter + markdown instructions) so they're portable to other tools.
"""
